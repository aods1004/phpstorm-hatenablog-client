{{ '{{' }} app_entry({
  url: "{{ url }}",
  content_type: "{{ content_type }}",
  title: "{{ title|meta_data }}",
  updated: '{{ updated|raw }}',
  category: [{% for i in category %}'{{ i|meta_data }}',{% endfor %}],
  draft: '{{ draft|raw }}',
}) {{ '}}' }}
{{ content|md_content }}
